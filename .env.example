# Optional: without DATABASE_URL the app uses in-memory storage.
DATABASE_URL=
# Optional Google Calendar integration (server-side credentials)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REFRESH_TOKEN=
